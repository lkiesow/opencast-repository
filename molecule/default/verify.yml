---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: Uest if opencast is available
    ansible.builtin.package:
      name: opencast
      state: present
